# Files excluded from the EAS build archive (not needed to compile the app)

# Store listing / marketing (only the Play Store text files, NOT the Zustand store source)
store/listing/
PRIVACY_POLICY.md
*.code-workspace

# Root-level images (feature graphics, etc. — not app assets)
aptipfeature.png

# Docs and web hosting
*.md
index.html
